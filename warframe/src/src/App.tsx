import React from 'react'
import { TrackerPage } from './pages/TrackerPage'

/**
 * Root app component.
 * Routing can be added here later (e.g. react-router) if an admin page
 * or history view is added as a separate route.
 */
function App() {
  return <TrackerPage />
}

export default App
